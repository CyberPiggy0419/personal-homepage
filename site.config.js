/* ────────────────────────────────────────────────────────────────
   站点配置 —— 只改这一个文件就能把主页接到你的 GitHub。
   Site config — edit this file to hook the page up to your GitHub.
   ──────────────────────────────────────────────────────────────── */
window.SITE_CONFIG = {
  // 1) 你的 GitHub 用户名（必填）。填好后页面上所有 GitHub 链接自动生效。
  //    Fill in your GitHub username — every GitHub link on the page picks it up.
  githubUsername: "CyberPiggy0419",

  // 2) 存放本页面的仓库名。用 <用户名>.github.io 做主页时留空字符串 ""。
  //    Repo that hosts this page. Use "" when the repo is <username>.github.io.
  githubRepo: "personal-homepage",

  // 3) 联系邮箱 / contact email
  email: "gezell1q@sjtu.edu.cn",

  // 4) 是否在页面上显示手机号。公开网页建议改成 false。
  //    Show the phone number on a public page? Set to false to hide it.
  showPhone: true,

  // 5) 站点地址（可选，用于 canonical 链接），例如 "https://xxx.github.io/"
  siteUrl: "https://cyberpiggy0419.github.io/personal-homepage/"
};
